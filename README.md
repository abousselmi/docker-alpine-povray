# Docker Alpine POV-Ray
An Alpine based docker image for POV-Ray.

## Run it:

### Using sh

docker run -it --rm -v $PWD:/src abousselmi/docker-alpine-povray sh

### Using bash

docker run -it --rm -v $PWD:/src abousselmi/docker-alpine-povray bash
